name: histocartography
dependencies:
  - python>=3.6
  - pip
  - pip:
    - torch
    - torchvision
    - pytorch-lightning==0.4.8 # --no-deps
    - mlflow>=1.2.0
    - tqdm>=4.35.0
    - boto3
    #- -e ../../ # install histocartography in editable mode